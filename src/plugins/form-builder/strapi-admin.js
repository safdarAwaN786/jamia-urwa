console.log('Loading form-builder admin plugin');

export default {
  register(app) {
    // console.log('Form Builder admin plugin registered');
  },
  bootstrap(app) {
    // console.log('Form Builder admin plugin bootstrapped');
  },
  async registerTrads({ locales }) {
    return [];
  }
};